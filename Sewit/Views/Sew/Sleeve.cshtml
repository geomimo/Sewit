@model List<SleeveComponentVM>


<div class="container">
    <div class="container-fluid">
        <div class="progress">
                <div class="progress-bar" role="progressbar" style="width: 75%" aria-valuenow="75" aria-valuemin="0" aria-valuemax="100">Sleeve</div>
        </div>
    </div>
    <br />
    <h2>
        Choose sleeve component
    </h2>
    <hr />
    <div class="row">
        @foreach (var comp in Model)
        {
            <div class="col-3">
                <div class="card card-bg flex-fill shadow">
                    <a asp-controller="Sew" asp-action="Recommend" asp-route-id=@comp.SleeveId>
                        <img class="card-img-top img-choice" src=~/@comp.PhotoPath />
                    </a>
                </div>
                <div class="card-body">
                    <h5>@comp.Name</h5>
                </div>
            </div>
        }
    </div>
</div>